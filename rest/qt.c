#include <stdio.h>

int main(void){

    int distanceFromGod = 0;

    int QT, pray, readBible;
    scanf("%d%d%d", &QT, &pray, &readBible);
          //put 0 or 1  ex) 111
    if(QT && pray && readBible){
        return distanceFromGod--;
    }
    else if((QT && pray) || (QT && readBible) || (pray && readBible)){
        return distanceFromGod;
    }
    else{
        return distanceFromGod++;
    }
}

char THE_SPIRIT_OF_THE_LORD_IS_ON_ME;



